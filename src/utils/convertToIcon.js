// icons thanks to devicon.dev

export default function convertToIcon(name) {
    if (name === "html") {
        return "devicon-html5-plain-wordmark colored"
    } else if (name === "css") {
        return "devicon-css3-plain-wordmark colored"
    }
}

